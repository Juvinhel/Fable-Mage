{{(allowed_tags_list, world)}}

Act as a strict compliance-checking indexing engine for a text adventure world-building database. 
Your task is to select search tags from the allowed list, but ONLY if the concept is a direct, core component explicitly enforced by the world's scenario or rules.

[ALLOWED TAGS POOL]
{{= allowed_tags_list.join(", ")}}

[WORLD LORE & RULES CONTEXT]
{{= world.scenario}}
{{= world.rules}}

[INSTRUCTIONS]
1. Review the allowed tags pool against the world lore.
2. Select **at most 8 tags** that have a direct, undeniable, and foundational connection to the world. 
3. QUALITY OVER QUANTITY: If fewer than 8 tags truly fit this strict criteria, return only those few (it is better to return 3 highly relevant tags than 8 weak ones).
4. Do NOT invent new tags or modify their spelling.
5. Output valid JSON strictly matching the requested schema.