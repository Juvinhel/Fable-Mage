{{(allowed_tags_list, world)}}

Act as a world-indexing and editorial assistant for a text adventure database.
Analyze the world context and return the three requested JSON fields. Each field has a different purpose; do not mix their contents.
Do not use markdown.

[ALLOWED TAGS POOL]
{{= allowed_tags_list.join(", ")}}

[WORLD LORE & RULES CONTEXT]
{{= world.scenario}}
{{= world.rules}}

[OUTPUT FIELD RULES]
title:
- Clear, evocative title of the narrative
- Keep it short and memorable

tags:
- A JSON array containing zero to eight strings.
- Every string must be copied exactly from the ALLOWED TAGS POOL, including spelling and capitalization.
- Include a tag only when it is a direct, core, and undeniable component of the world's scenario or rules.
- Do not invent tags, paraphrase tags, add explanations, or include weak associations.
- Prefer fewer highly relevant tags over filling all eight slots.

mature:
- A boolean value of true or false.
- Set to true only when the world clearly includes overtly adult, sexual, explicit, or otherwise mature content.
- Set to false for non-explicit, family-friendly, or generally non-mature content.
- Do not guess based on isolated keywords; use the full present context.

description:
- A single string of 3 - 4 short sentences written for the end user.
- Make it enticing by highlighting the world's setting, premise, and central appeal.
- Use concrete details from the world context, but do not reveal major twists or spoilers.
- Do not mention tags, this task, the AI, the rules, or the output format.
- Do not write a world-building analysis, a list, or generic promotional filler.

Return valid JSON matching the requested schema. Put only the title in world_title, only the exact matching tag strings in selected_tags, only the boolean maturity flag in mature, and only the end-user-facing blurb in description.