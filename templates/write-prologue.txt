{{(input, world)}}
{{ 
    function convertCase(input)
    {
        return input.replace(/(^\w|-\w)/g, clearAndUpper);
    }    
    
    function clearAndUpper(text)
    {
        return text.replace(/-/, "").toUpperCase();
    }
}}

Act as a master fantasy and sci-fi world-builder and narrative designer.
Write a prologue for a fictional roleplay in valid JSON format.
Use the scenario described in the 'scenario' block and the player character described in the 'player' block. 
Use the user input in the 'user_input' for the general direction where the story should start off.
Focus on core aspects described in the 'focus' block.
You may include npcs described in the 'npc' blocks.

Strictly adhere to the following length constraints and use exactly these properties:
    - plot: Prologue (3 to 5 paragraphs, overall 200 to 400 words). Refer to the player as 'you'. Include thoughts and physical sensations of the player.
    - unseen: Thoughts and reasoning of non player characters and events happening unknown to the player (100 to 200 words). Avoid elaborate or verbose language.
    - imagery: (75 tokens) Optimized, comma-separated text-to-image prompt of one key visual of the storyline. Avoid filler words.

<scenario>
{{= world.scenario }}.
</scenario>

<focus>
{{= world.focus }}.
</focus>

<player>
{{~ [key, property] of Object.entries(world.player) }}
{{= convertCase(key) }}: {{= property }}
{{~}}
</player>

{{? world.npcs }}
{{~ npc of world.npcs }}
<npc>
{{~ [key, property] of Object.entries(npc) }}
{{= convertCase(key) }}: {{= property }}
{{~}}
</npc>
{{~}}
{{?}}

<user_input>
{{= input }}
</user_input>